<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/yellowBase</item>
        <item name="colorPrimaryDark">@color/greenBase</item>
        <item name="colorAccent">@color/orangeBase</item>
    </style>

</resources>
